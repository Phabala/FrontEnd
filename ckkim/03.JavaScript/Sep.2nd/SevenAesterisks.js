const prompt = require("prompt-sync")();
let input = prompt('점수 입력(0~100): ');
input = Number(input);

for (let)

// 홀수를 인풋 값으로 받고
// (홀수-i) 만큼 공백 넣고, i 만큼 별 넣고, 다시 (홀수-i)만큼 공백을 넣는다.
// 만약 i가 input이 되면 그 다음에는
// 